# Folder: repository root

1. **地位**：Chat Monorepo、质量任务、部署入口与全局文档的根。
2. **逻辑**：`apps/` 装配可部署应用，`packages/` 提供领域与 adapter，`docs/` 保存横切架构；根部署文件把同一代码产出 Vercel 或 Docker profile。
3. **约束**：根层只保存全局配置、部署入口和总览；业务实现必须进入职责明确的 app/package。迁移必须显式执行，秘密不得进入镜像或 Git。

## 成员清单

- `apps/`：可部署应用。
- `packages/`：领域、adapter、UI 与共享配置。
- `docs/`：研究与架构事实源。
- `.agents/`：项目级 Agent Skills。
- `.github/`：GitHub CI 与协作配置。
- `.env.example`：无秘密的认证、部署与首模型 bootstrap 环境模板。
- `.dockerignore`：Docker build context 排除规则。
- `Dockerfile`：standalone Web runner 与显式 migration target。
- `compose.yaml`：PostgreSQL、migration、Web 本地/自托管编排。
- `package.json`、`bun.lock`、`turbo.json`：workspace、依赖锁/安全 override 与任务图；根开发/迁移命令显式加载被 Git 忽略的 `.env`。
- `README.md`、`design.md`、`AGENTS.md`：项目入口、设计基线与工程协议。

**触发器**：根成员、部署入口、workspace/任务图或全局文档变化时同步本文档、README、design 与对应深度文档。
