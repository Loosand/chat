# Folder: apps

1. **地位**：可部署应用入口集合；当前只有 Next.js Web。
2. **逻辑**：应用负责装配内部 package，并把 HTTP、页面和运行时生命周期连接到领域能力。
3. **约束**：业务规则不得只存在于页面或 Route Handler；新增应用必须说明独立部署价值。

## 成员清单

- `web/`：Next.js 用户端、未来管理端和 API 入口。

**触发器**：新增、删除应用或修改部署边界时，同步更新本文档、根 README 和 `design.md`。
