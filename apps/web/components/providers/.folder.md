# Folder: apps/web/components/providers

1. **地位**：供应商管理页面所需的最小浏览器交互组件。
2. **逻辑**：只处理确认等浏览器状态；数据读写仍由 Server Action 与领域服务负责。
3. **约束**：不得接收 API Key、密文或 ProviderConnectionRecord。

## 成员清单

- `delete-provider-connection-form.tsx`：删除前本地确认并提交 owner-scoped Server Action。

**触发器**：组件职责、提交动作或浏览器状态变化时同步本文档和父地图。
