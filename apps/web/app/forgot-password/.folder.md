# Folder: apps/web/app/forgot-password

1. **地位**：密码重置邮件申请页面。
2. **逻辑**：浏览器调用 Better Auth request-password-reset，并统一显示防枚举成功状态。
3. **约束**：无论邮箱是否存在都不能从 UI 结果区分。

## 成员清单

- `page.tsx`：重置申请页面与登录返回入口。

**触发器**：密码恢复流程或文案变化时同步本文档和 auth.md。
