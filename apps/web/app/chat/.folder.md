# Folder: apps/web/app/chat

1. **地位**：认证用户聊天产品入口。
2. **逻辑**：当前 Server Component 先读取权威 Better Auth session，未认证跳转登录，并提供退出当前 session 的操作；下一原子功能接入聊天 client。
3. **约束**：不得信任客户端 owner；页面始终由 server session gate 保护；尚未实现的聊天交互不能提前宣称完成。

## 成员清单

- `page.tsx`：受保护的最小聊天入口壳页。

**触发器**：session gate、聊天页面职责或子路由变化时同步本文档、父地图和 auth/frontend 文档。
