# Folder: apps/web/app/chat/[conversationId]

1. **地位**：持久 conversation 的刷新恢复页面。
2. **逻辑**：Server Component 并行读取 session owner/params，再加载 owner-scoped snapshot、active run 与公开模型，交给 ChatWorkspace。
3. **约束**：URL 只标识 conversation；owner 永远来自权威 session；不存在或越权使用相同领域错误边界。

## 成员清单

- `page.tsx`：持久 conversation 首屏和 active run 恢复入口。

**触发器**：params、首屏加载、恢复或错误边界变化时同步父地图和 frontend-stack/chat-http 文档。
