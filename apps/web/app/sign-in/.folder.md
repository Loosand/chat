# Folder: apps/web/app/sign-in

1. **地位**：邮箱密码登录页面。
2. **逻辑**：Server Component 只组合 AuthCard 与浏览器 EmailAuthForm。
3. **约束**：成功登录导航 `/chat`；不从 query 接受任意 redirect 目标。

## 成员清单

- `page.tsx`：登录页面与注册/找回入口。

**触发器**：登录流程、文案或导航变化时同步本文档和 auth.md。
