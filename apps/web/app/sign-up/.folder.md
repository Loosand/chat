# Folder: apps/web/app/sign-up

1. **地位**：邮箱密码注册与验证引导页面。
2. **逻辑**：Server Component 组合 AuthCard；浏览器表单提交后进入统一邮箱验证等待态。
3. **约束**：不披露邮箱是否已存在；不在 URL 或客户端状态保存密码。

## 成员清单

- `page.tsx`：注册页面与登录返回入口。

**触发器**：注册/验证流程或文案变化时同步本文档和 auth.md。
