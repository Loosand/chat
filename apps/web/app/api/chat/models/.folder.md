# Folder: apps/web/app/api/chat/models

1. **地位**：聊天可选平台模型的公开只读目录。
2. **逻辑**：先确保可选环境 bootstrap 已幂等补齐，再只返回 enabled/public 且拥有完整 enabled route 链路的 chat 模型展示/capability 字段。
3. **约束**：不得返回 upstream、binding、route、system prompt 或 credential reference。

## 成员清单

- `route.ts`：`GET /api/chat/models`。

**触发器**：公开字段、任务过滤或可用性语义变化时同步本文档和模型目录文档。
