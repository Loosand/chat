# Folder: apps/web/app/api/auth/[...all]

1. **地位**：Better Auth 所有 HTTP endpoint 的 catch-all 叶节点。
2. **逻辑**：GET/POST 统一转发给请求时构造的 Better Auth standard handler。
3. **约束**：保持 Node runtime 和动态执行；不得添加旁路鉴权或自动 migration。

## 成员清单

- `route.ts`：Better Auth GET/POST Route Handler。

**触发器**：处理方法、runtime 或 auth handler 变化时同步本文档、上级地图和测试。
