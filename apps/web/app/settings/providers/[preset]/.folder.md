# Folder: apps/web/app/settings/providers/[preset]

1. **地位**：单个 ProviderPreset 的用户级配置表单。
2. **逻辑**：Server Component 校验 preset、读取脱敏连接状态，并装配保存、检查和删除操作。
3. **约束**：更新时空 API Key 表示保留旧密钥；未配置 credential vault 时禁用写入和检查。

## 成员清单

- `page.tsx`：兼容 Base URL、模型 ID、启停、密钥替换、检查结果和危险操作 UI。

**触发器**：详情页面字段、动作或状态表达变化时同步本文档和父地图。
