# Folder: apps/web/app/settings

1. **地位**：认证用户的个人设置入口集合。
2. **逻辑**：各子路由从权威 server session 推导 owner，并只装配 owner-scoped 应用服务。
3. **约束**：这不是管理员后台；页面不得接收、回显或缓存供应商密钥与密文。

## 成员清单

- `providers/`：五种兼容供应商的用户级配置、检查与删除入口。

**触发器**：增加设置类别、改变 session gate 或设置数据归属时同步本文档、父地图和相关架构文档。
