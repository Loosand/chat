# Folder: apps/web/app/reset-password

1. **地位**：一次性 token 密码更新页面。
2. **逻辑**：客户端从 Better Auth redirect query 读取 token 并提交新密码。
3. **约束**：token 不进入日志/文案/持久状态；缺失或失败只显示固定安全提示。

## 成员清单

- `page.tsx`：密码更新页面与登录返回入口。

**触发器**：token、重置流程或文案变化时同步本文档和 auth.md。
