# Folder: packages/jobs/src

1. **地位**：Trigger.dev、BullMQ 和 Inline adapter 未来共同实现的后台任务 contract。
2. **逻辑**：当前定义稳定 job 名、payload 泛型、幂等/并发选项和 driver 操作。
3. **约束**：不包含具体 Trigger task；Web 只能依赖 contract，不能导入任务实现。

## 成员清单

- `index.ts`：JobDriver、状态、触发选项与 handle contract。

**触发器**：任务状态、幂等语义、取消或 driver 能力变化时同步更新本文档、design.md 和任务架构文档。
