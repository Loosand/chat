# Folder: packages/auth/scripts

1. **地位**：认证生成物的可复现工程脚本目录。
2. **逻辑**：固定 CLI 参数生成临时 schema，补充项目 Header 后格式化到 database package。
3. **约束**：只生成文件，不连接或迁移任何数据库；目标必须保持可重复生成。

## 成员清单

- `generate-schema.ts`：Better Auth CLI 到 `packages/database/src/auth-schema.ts` 的封装。

**触发器**：CLI、输出路径、生成命令或 Header 协议变化时，同步本文档、父目录地图与认证架构文档。
