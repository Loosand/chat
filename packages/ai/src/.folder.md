# Folder: packages/ai/src

1. **地位**：AI SDK 与未来 provider/protocol adapter 的纯模型调用边界。
2. **逻辑**：当前用 `streamChatText` 验证 AI SDK 流式调用 contract；后续在此归一化 provider 事件、usage 与错误。
3. **约束**：不访问数据库、缓存、计费、任务系统或 Next.js；不负责选择平台 route。

## 成员清单

- `stream-chat-text.ts`：最小 AI SDK `streamText` 封装。
- `index.ts`：AI package 公共导出。

**触发器**：模型输入输出、provider、协议或公共导出变化时同步更新本文档、文件 Header 和相关架构文档。
