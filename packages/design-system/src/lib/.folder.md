# Folder: packages/design-system/src/lib

1. **地位**：design-system 内部与公开的无状态 UI 工具目录。
2. **逻辑**：为 shadcn/ui 源码提供稳定、可复用的底层 helper。
3. **约束**：不得包含浏览器状态、业务规则、网络请求或基础设施访问。

## 成员清单

- `utils.ts`：使用 clsx 与 tailwind-merge 实现 `cn`。

**触发器**：工具增删、签名、依赖或公开路径变化时更新本文档和上级地图。
